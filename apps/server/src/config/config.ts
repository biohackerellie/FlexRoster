import { env } from "~/env";

export const secretaries = env.SECRETARIES;

export const preferredNames = env.PREFERRED_NAMES;

export const excludedTeachers = env.EXCLUDED_TEACHERS;

export const semesterClassName = env.SEMESTER_CLASS_NAME;

export const isRedisCluster = env.IS_REDIS_CLUSTER;
