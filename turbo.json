{
	"$schema": "https://turborepo.org/schema.json",
	"globalDependencies": ["**/.env"],
	"pipeline": {
	"build":{
		"dependsOn": ["^build"],
		"outputs": [
			"dist/**",
			"!.next/cache/**",
			"next-env.d.ts",
			".output/**"
		]
	},
	"dev": {
		"persistent": true,
		"cache": false
	}
}
}